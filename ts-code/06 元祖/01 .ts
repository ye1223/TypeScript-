/* // 数组
const person: (string | number)[] = ['张三', 'student', 18] //数组中数组位置可以变动

// 元祖
const person2: [string, string, number] = ['李四', 'teacher', 45] //内容不能多或少，内容顺序不能被改变？
// const person3: [null, string] = ['a', null]

// csv数据
const data: [string, string, number][] = [
    ['1', '1', 1],
    ['2', '2', 2],
    ['3', '3', 3]
]

console.log(person2[2])

 */